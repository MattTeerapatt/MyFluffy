import 'package:flutter/material.dart';

class CharitySection extends StatelessWidget {
  const CharitySection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("CharitySection"),
    );  
  }
}