import 'package:flutter/material.dart';

class ForumViewSection extends StatelessWidget {
  const ForumViewSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("ForumViewSection"),
    );
  }
}