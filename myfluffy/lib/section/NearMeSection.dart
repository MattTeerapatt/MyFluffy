import 'package:flutter/material.dart';

class NearMeSection extends StatelessWidget {
  const NearMeSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("NearMeSection"),
    );
  }
}